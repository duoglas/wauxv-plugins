---
name: project-status
description: 当前部署版本 + 挂起的真机收尾项 + 迁移状态 (每 session 先看这条接上工作)
metadata: 
  node_type: memory
  type: project
  originSessionId: 3cbb9098-fbe3-4e37-8615-e1be36f085db
---

**截至 2026-06-11。换机器/新 session 先看这条接上工作。**

## 大工程进行中:GroupAdmin + RedPacketStats 合并(方案 B + 强制本地测试)
用户要把红包统计合并进群管,成**一个**插件、直接共享数据/逻辑(伸手党→直接 doWarn,免读库/发命令往返)。约束:不要单一巨型 main.java,**领域模型设计 + 所有功能必须本地单测+集成测试**。
- **方案 B**(定):源码按领域分模块 + `build.sh` 拼接成单一 main.java(WAuxiliary 运行时=单文件 BeanShell,弱 OO 跑不了真 class 领域模型,故领域模型放源码层、构建期拼接)。
- **可测性**:端口与适配器(HostPort/StoragePort/Clock),领域只依赖端口;生产接 android、测试接 fake/**sqlite-jdbc 真库**。洞察:**BeanShell 本机也能执行**(同设备解释器),+ sqlite-jdbc 跑同一份 SQL → 能本地抓 `min/max(NULL,x)` 类 bug。
- **测试金字塔**:L1 单测(领域 vs fakes,纯 bsh)/ L2 集成(模块+sqlite-jdbc 真库+fake 宿主)/ L3 装配体 bsh 解析 / L4 真机(只剩 WeChat 反射/sendText/scrcpy)。
- **设计文档**:`docs/superpowers/specs/2026-06-09-merge-groupadmin-redpacket-design.md`(架构+测试+迁移+分期 P0-P3)。
- **config 事实(已核实)**:config.prop **按插件隔离**,RP 全 `rp_` 前缀与 GA 键零冲突 → 合并 config 可 union;合并后须停旧 RP 插件防红包双重处理。见 [[waux-config-per-plugin-isolated]]。
- **✅ P0 完成(2026-06-09, commit 8c3e89c, `merged/` 不碰线上)**:build.sh + 端口/适配器 + 本地 runner + check.sh 门禁 + 证明缝 `speak_store` UPSERT;突变验证 red→green(去 coalesce 两测全红→还原全绿,真能抓那个咬过两次的 NULL bug);sqlite-jdbc 用 **3.36.0.3**(3.43+ 硬依赖 slf4j);bsh.Interpreter 异常仍 exit 0 → runner 查 TEST_OK 哨兵。`cd merged && tools/fetch-deps.sh && ./check.sh`。
- **✅ P1-a 完成(2026-06-09, membership 领域, 纯本地未碰线上)**:`merged/src/domain/membership.bsh`(canActOn/isOwner/isAdminInGroup/isNativeOwner + getOwners/getAdmins/getNativeOwners, 只依赖 HOST 端口, 键名逐字保真:`super_owners`/`admins_<normGid>`/`nat_owner_<裸gid>`) + `merged/src/core/host_android.bsh`(生产骨架, detectNativeOwner=TODO P2 移植 GA@785 反射) + `merged/tests/fakes/host_fake.bsh`(HostPort 内存 fake) + `merged/tests/membership_unit.bsh`(canActOn 真值表 10 例) + manifest 接入。`cd merged && ./check.sh` GREEN(build+parse+3 测全绿), 突变验证案例3 red→green。HostPort 契约: getString(k,def)/getLoginWxid()/detectNativeOwner(grp)。
- **✅ P1-b 完成(2026-06-09, warning 领域, 纯本地)**:`merged/src/domain/warning.bsh`(getWarn/setWarn/getWarnKick/setWarnKick/getWarnList/add/del + botPrivState + doWarn(3参/4参)/doWarnDec/doWarnClear, 逐字保真 GA@2712, 键用裸 gid:`wn_/wk_/wl_/wnt_/wnf_/bp_`) + membership.bsh 追加白名单谓词(isWhitelistAny/getWhitelist/isGlobalWhitelist + joinCsv) + `merged/src/core/clock_android.bsh`(CLOCK 端口) + host_android/host_fake 扩 sendText/lookupName/tryKickSilent 缝 + `merged/tests/warning_unit.bsh`(doWarn 真值表 9 场景/20 断言:计数/到顶踢/踢成败清零/自动豁免管理/白名单/无权/wk_覆盖)。check.sh GREEN(4/4 测), 突变 `n>=kick`→`n>kick` red→green。端口增:HOST.sendText/lookupName/tryKickSilent(后两生产骨架 TODO P2 移植反射 GA@1917/@691), CLOCK.now()。
- **✅ P1-c 完成(2026-06-09, speak 领域)**:`speak_store.bsh` 扩 last_speak —— `SPEAK_UPSERT_LASTSEEN_SQL`(线上 flush @437-439 逐字: `last_speak=max(coalesce(...),excluded)`+`first_seen=min(coalesce(...),excluded)`, v1.19.2/v1.20.0 coalesce 教训) + `speakRecord(grp,wxid,ts)` + `speakQueryLastSpeak`; 新 `merged/src/domain/speak.bsh` 纯函数 `lurkClassify(firstSeen,lastSpeak,now,lurkDays)`→0 NO_BASELINE/1 NEW_EXEMPT(进群<1天 LURK_NEWMEMBER_EXEMPT_MS)/2 ACTIVE(lastSpeak>now-days)/3 LURKER(@1466-1509 逐字)。L1 `lurk_classify_unit`(10 断言含边界) + L2 `speak_lastspeak_integration`(sqlite-jdbc 坐实 coalesce NULL bug: 去 coalesce→last_speak 卡 NULL 变红)。check.sh GREEN 6/6。**recordSpeak 异步缓冲/延迟flush/降级(L3_dirtyLs 热路径 infra)本期未抽, 留 hooks 集成**。
- **✅ P1-d 完成(2026-06-09, app 层装配, 纯本地, commit 待定)**:`merged/src/app/` 四模块 —— `speak_buffer.bsh`(recordSpeak L3 异步缓冲/l3Flush/降级状态机, 逐字保真 GA@710/@403-514, 落库走 STORAGE 端口)、`commands.bsh`(onHandleMsgBody 文本命令路由 26 命令 + dispatchAction, 反射输入用占位缝, doWarn/doWarnClear/doWarnDec 已抽 domain 真调)、`dialogs.bsh`(设置 UI onClick @2935-4022)、`hooks.bsh`(onLoad 端口接线 makeAndroidStorage/Host/Clock + 建表 + l3FlushLoop 启动 / onHandleMsg 包装层采集→路由 / onUnload; 据实发现线上**无 onActivityResume**只有 onLoad/onUnload/onHandleMsg/onMemberChange)。各模块 L1/L3, check.sh GREEN; 独立 Layer3 审查 commands+hooks 对线上**0 CRITICAL**。**P2 必盯缝**: commands 占位 `_cmdGroupEnabled`(读 ge_ 键) vs hooks 真调 isGroupEnabled, P2 接线须接回同源。
- **✅ 伸手党合并后判定逻辑就位(2026-06-09, P3-ahead, 纯本地)**:`merged/src/domain/freeloader.bsh`(`freeloaderDecide` 纯函数 + `freeloaderJudge` 驱动) + speak_store `speakSnapshotAll` + 端口扩 `queryRows`(jdbc/android/fake 三实现 NULL 一致, 真库探针证 NULL INTEGER→Java null) + L1(freeloader_unit 17 断言)/L2(freeloader_judge_integration 36 断言)/L1(speak_snapshot_fake_unit)。check.sh 11/11 绿。**独立 Code Review PASS 无 CRITICAL/MAJOR**, 降级红线(snap=null/无此人/last_speak NULL 三条)mutation 证非假绿。设计见 `docs/superpowers/specs/2026-06-09-红包伸手党-合并后判定重设计.md`。**只做了纯判定层**; 设备侧(P+G 延迟拍照调度/拍照终结竞态#24/G运行期夹取#26/反射取领取者/l3Flush接线/doWarn直调/since宽限/均分跳过/端到端踢人#25)留 P3 真机集成。
- **✅ P2-a 完成(2026-06-09, 红包领域逻辑抽进 merged/, 纯本地, commit 待定)**:四片各独立文件 —— `redpacket_store.bsh`(rp_record 表 + 录入 rpRecord/当日查询 rpQueryToday/按群聚合, 走 STORAGE; 注意线上无 SQL GROUP BY, 聚合是 Java 层 LinkedHashMap, 已逐字保真 SELECT)、`redpacket_tiers.bsh`(getTiers/getTiersByType per-group 1-10 档 `rp_tiers_<gid>`/定制回退, tierOf 选档**严格>**, v1.6.0a 未设档不泄漏; RP 的 normGroupId 仅 trim 与 GA 不同, 用本地 rpNormGroupId 保真)、`redpacket_qualify.bsh`(达标 tierOf>=1 / qualifiers 序列化 `昵称|金额|档;` / 拼手气vs均分 rpIsEqualSplit / 排除名单 `rp_exclude_<gid>` / 定制 rpIsCustom title.contains)、`redpacket_export.bsh`(rpBuildGroupedMsgs 按群多行组装/MAX_CHARS 1800 分条 / 转发目标按群 `rp_export_target_<gid>` 回退全局, 默认 DEF_EXPORT_TARGET=线上既有 wxid)。L1/L2 全测矩阵(每片红→绿 mutation)。check.sh GREEN 15/15。manifest 按依赖序整理。**反射/UI/worker/Job调度/sendText 留设备 seam(P-device)**。
- **✅ P2-b 完成(2026-06-09, cutover 只设计不执行, 纯本地产出)**:`merged/cutover/` 三文件 —— `README.md`(runbook: 前置条件/决策表 C1-C5/执行顺序/验收/回滚/不可逆清单)、`cutover.sh`(迁移脚本, POSIX sh 设备侧 su 跑, **硬闸门 CUTOVER_CONFIRM=yes** 防误跑: 校验前置→备份到 Plugin 同级备份区→config union(GA 全量+RP rp_ 键, 校验键数相等)→**拷贝**(非移动)两 DB 进 GroupAdminPlus(含 -wal/-shm)→移走旧 GA+RP 目录停用→提示重启微信)、`rollback.sh`(ROLLBACK_CONFIRM=yes + BACKUP_DIR, 停 GroupAdminPlus/恢复旧两插件回 Plugin/)。`sh -n` 双绿, Security grep 干净, 零真机动作。**关键决策**: C3 DB 拷贝进 GroupAdminPlus(旧库原地=回滚锚点, 代价=回滚丢 cutover 后增量) + 合并插件读自己目录(须与 P3 hooks.bsh STORAGE 接线对齐, 若改绝对路径读旧库则 C3/C4 冲突要重审); C4 旧 GA+RP **两个都停**(GA 不停=群管双跑/RP 不停=红包双重处理)。**⚠️ 禁止 P3 完成前运行**(反射/UI/调度仍 seam, 现在 cutover 红包会瘫)。真跑须另开 PLAN+用户在场 scrcpy+Santa 双审。
- **✅ P3-a 完成(2026-06-09, commit 7e06970, 纯本地零真机)**:`merged/src/app/redpacket_app.bsh` —— `rpProcessReceivers`(逐字对齐线上 hbDetailExtract: 均分跳过→达标 qualifiers→rpRecord 写库→分档+发送; 反射取领取者/sendQuote/随机语气/限频留 P3-b seam) + `freeloaderRunForPacket`(**伸手党合并核心收益**: since 闸门(since<=0 lazy-init 补写再整包跳过 / packetMs 在 since+winMs 内预热整包跳过 / 否则正常, 逐字对齐 v1.11.4) → **l3Flush in-process 消跨插件 flush 延迟(根治 v1.11.5 病根#3)** → speakSnapshotAll 单快照 → freeloaderJudge → toWarn 逐个 **doWarn(auto 来源)in-process 直调**, 删跨插件命令往返; reason 空串守护防 auto=false 误发"权限不足"; 降级红线不绕过)。端口 host_android 加 sendQuote(占位 TODO P3-b)。L2 全链路 `redpacket_app_freeloader_integration`(since#16/reason 守护/#8d NULL+非新人→WARN/到 wk_ 阈值踢 captured) + `redpacket_app_process_integration`。check.sh GREEN 17/17(独立复跑确认 3301 行/解析 EXIT=0), 新逻辑红→绿 mutation 验非假绿, Security 干净。独立 Code Review PASS 0 CRITICAL, 抓 2 MAJOR(since seam 未声明/reason 空串)已修。**P3-b seam**: 反射取领取者(hbFindReceiverList/hbExtractTriple)/sendQuote 健壮回退/T_snap=P+G 延迟调度+G夹取+拍照终结竞态#24/per-group 配置读/@条随机语气+限频+间隔延迟。
- **P3-b 进行中(设备 seam 移植, 纯本地, 零真机)**:9 任务拆 T1-T9, 决策已定=多模块拆分(app/device_*+redpacket_detect/worker)+每2-3任务一commit。计划见 `.harness/current-plan.md`。
  - **✅ T1-T3 完成(2026-06-09, commit f558d74)**:
    - **T1** `merged/src/app/device_base.bsh`(197行):RP 设备层共享声明逐字保真(UI常量/RP_QUEUE+JOB_*10槽索引/SEEN/DONE/OPENED/COVER_ACT/IN_FLIGHT/CUR_JOB/FREELOADER_SNAP/PERF_*全套埋点/RP_DB句柄/hbBgLog/hbNow/hbJitter)。**刻意未搬**(domain已声明,避顶层重声明):FREELOADER_MAX_WARN(在 freeloader.bsh:22)+全部 K_*/DEF_*(tiers/qualify/export 用 RP_K_*/RPQ_K_*)。
    - **T2 最高风险·检测热路径合一** `redpacket_detect.bsh`+改 hooks.bsh:`rpDetectAndEnqueue` 并入 onHandleMsgBody, **一条消息同时过 GA(recordSpeak发包人+routeCommand)与 RP(检测入队)互不破坏**——红包content空→GA非文本空content分支记发包人;content含nativeurl(非空)→落routeCommand主路径@2217记发包人;两种都记到,与线上GA逐字一致。RP 用**独立 `rpIsGroupEnabled`(rp_enabled_groups+RP_ENABLED_CACHE TTL60s)**与GA `isGroupEnabled`(enabled_groups)解耦不撞名。热路径O(1):type/indexOf/缓存查/去重/入队≤50,零XML解析,getLoginWxid仅content含"红包排除"才调。**不搬 v1.11.5 检测时快照线程(@1281-1318)**——合并版改T5 in-process T_snap拍照。onHandleMsg包装层纳入RP A/B分类计时(PERF_LAST_RP)。
    - **T3** `redpacket_worker.bsh`(397行)+L1测:`rpWorkerTick`(单飞tick+看门狗30s强清+dueAt<=now挑最早出队+finally自重调度绝不停摆)/`hbProcess`(attempt0:专属早退exclusive_recv_username→nativeurl→rpExtractTitle回填JOB_TITLE+PERF_EXTRACT计时→rpIsCustom回填JOB_ISCUSTOM→开封面页)/`afterCoverNoDetail`三段重试阶梯(放弃点attempt>=3,attempt0→retry1/1→retry2/2→retry3末档1h,hbJitter±25%)/`rpExtractTitle`+`hbGetElement`(XML,纯函数,加了L1单测14断言)/`perfFlush`真实落盘(收口hooks占位,装配体单定义)/`rpDailyCheck`(日期闸门真实移植,发送部分置seam→T6)。
  - **seam 待后续接**:UI启动器`rpOpenCover`/封面finish`rpFinishCover`(T4)、反射领取者hbDetailExtract+onResume链路(T4/T5)、~~发送`rpDailySend`/hbTierAndSend/rpSendQuote/rpSendAt(T6)~~✅、~~`rpHandleExcludeCmd`(T7)~~✅、isGroupEnabled同源收口(T8)。
  - **✅ T6 完成(2026-06-09 深夜, 纯本地零真机, commit 待定)**:发送层逐字保真移植 —— 新 `merged/src/app/redpacket_send.bsh`(发送层主体): `rpRecordStats`(写库, 选档用getTiers普通档@2224, 达标复用domain rpComputeQualified, 落库走STORAGE rpRecord) + `hbTierAndSend`(3参/4参重载, 分档+两条提醒: 引用群规条HOST.sendQuote/@条HOST.sendText, 限频cfgMsgGap+RP_LAST_MSG, >atLimit汇总notice vs ≤逐档@, 随机语气pickConnector/pickActionPhrase, 两条间隔cfgTwoMsgGap延迟delay, 选档用getTiersByType@2993 — **写库getTiers/发送getTiersByType刻意非对称, 线上原样保留不修**) + appendTierLine + rpGroupName/rpSenderName(反射leaf) + getDailyGroups + rpDailySend(每日定时, 窗口查询走新domain rpQueryWindow/STORAGE, 组装domain rpBuildGroupedMsgs, 按群路由cfgExportTargetFor); 改 `host_android.sendQuote`接健壮回退(msgid>0引用→异常fallback sendText/msgid≤0直接sendText, 复刻线上rpSendQuote@1720-1736, 端口层无日志依赖); 加 domain `redpacket_store.rpQueryWindow`(ts范围+grp IN动态占位, trim规范化对齐线上@2557, SQL@2560逐字); 删两处leaf(device_recv rpDeliverReceivers / worker rpDailySend), 装配体唯一定义。check.sh GREEN 21/21(新增 redpacket_query_window_integration L2: ts边界>=/<严格+grp IN过滤+ORDER BY+trim+空列表心跳), 装配体解析EXIT=0(5313行), uniq -d仅有意arity重载(doWarn/dispatchAction/setWarnKick/hbTierAndSend), 跨模块依赖手工grep全存在, Security干净。**独立 Code Review PASS: 0 CRITICAL/0 MAJOR/1 MINOR**(MINOR=合并版发送在关页前 vs 线上发送在关页后, T5已commit结构device_recv.bsh:327, 发消息目标群会话与finishDetailAndCover正交不阻塞, 可接受)。**端口映射**: rpSendQuote→HOST.sendQuote/rpSendAt→HOST.sendText/写库→STORAGE/窗口查询→STORAGE.queryRows。
  - **✅ T7 完成(2026-06-10, 纯本地零真机, commit 929813b[W2+W3]+a8913d3[W4])**:RP 设置层全套逐字保真并入(线上 §22-§25)。**两 Implementer Agent 移植 + 我每步独立复跑 gate + 独立 Code Reviewer(Layer3) author-bias 复核**。
    - **W2+W3(设置 UI, +995行/0删)**:`dialogs.bsh`(+815, _rp* UI helper + showConfigDialog 主菜单 + 7子页 Basic/NormalTiers/Custom/Delay/Freeloader/Exclude/Forward + rpPickTarget/rpApplyForwardTarget/sendForwardNotice 后台getFriendList/getGroupList+Handler防ANR + rpRebuildExcludeRows + saveInt/Long/StrField), `redpacket_send.bsh`(+71, isDailyEnabled/enable·disableDailyGroup/rpExportToday端口化), `redpacket_qualify.bsh`(+33, setCustomOn/setCustomKw/removeExclude), `redpacket_tiers.bsh`(+51, cfgT1-3/cfgTxt1-3/rpParseTierBox), `redpacket_detect.bsh`(+25, rpEnableGroup/rpDisableGroup)。
    - **W4(命令层, +~440行)**:`commands.bsh`(+315, `rpOnClickSendBtn`=线上 onClickSendBtn@1333-1566 改名, 全 RP 配置命令 + rpCurrentTalker/rpCurrentGroupOrToast/rpStatusText), `redpacket_tiers.bsh`(+113, 档表命令 rpSetThresholds/SetTierAction/AddTier/DelTopTierCmd 各2签名), `redpacket_qualify.bsh`(addExclude), `redpacket_send.bsh`(rpDailyTest), `redpacket_detect.bsh`(rpHandleExcludeCmd 占位→真实体)。
    - **关键决策**:① `rpEnableGroup/rpDisableGroup` 提前落地(rp前缀避撞 GA enableGroup, 写 RP_K_ENABLED 与读侧 rpIsGroupEnabled 同源, §20 enableDailyGroup 副作用保留)——T8 isGroupEnabled 同源收口时复核。② rpExportToday/rpDailyTest 端口化(rpQueryToday/rpQueryWindow + HOST.sendText 镜像 rpDailySend, 文案/脱敏日志逐字)。③ **`rpOnClickSendBtn` 未接真入口**:与 GA `onClickSendBtn` 撞名, 双入口合一收口=T8(本期各存一份不撞)。
    - **符号改名表**(线上→合并):cfgInt→rpCfgInt / K_*→RP_K_* / tiersCustomKey→rpTiersCustomKey / enableGroup→rpEnableGroup / getString·putString·sendText·getLoginWxid→HOST.*。键全部读写同源。
    - **验证**:build EXIT=0(6806行) + check.sh 21/21 + uniq-d 仅 4 新预期 arity 重载(档表4命令)+3已知(dispatchAction/doWarn/hbTierAndSend), rpOnClickSendBtn 与 GA onClickSendBtn 各1不撞, 键同源, 0泄漏。独立 Review **READY 0 CRITICAL/0 MAJOR/2 MINOR**(非actionable: MINOR-1 排除命令 normGroupId 复核后判定当前正确——整个排除子系统一致用 membership normGroupId 读写同源, 改单点反破坏; 群id无空格两者等价)。Layer4 Santa降级豁免(非热路径/非破坏性)。
    - **★T7 给 T8 的盯点**: T8 onLoad/hook 合一时须把 RP `rpOnClickSendBtn` 接进真实 `onClickSendBtn`(同时认 GA「群管」触发词 + RP「红包统计设置」等), 解决双入口撞名; 并把 rpEnableGroup/rpIsGroupEnabled 与 GA enable/isGroupEnabled 的同源收口一并扫(rpHandleExcludeCmd 调用点已在检测路径, 无需再接)。
  - **★给T5的协调点**:合并版hbProcess执行时本包尚无FREELOADER_SNAP(改in-process T_snap=packetMs+G,晚于hbProcess);T3已删hbProcess三处终结路径(专属早退@1775/无hostCtx abort@1815/放弃点@2083)的`FREELOADER_SNAP.remove`并留注释;**T5实现freeloaderRunForPacket须自带快照拍照+清理(含终结路径兜底清/泄漏防护),不依赖worker侧remove**。
  - **教训(贯穿T4-T8)**:BeanShell解析门只查语法**不查符号解析**,跨模块缺失函数(parseCsv/rpNormGroupId类)解析仍EXIT=0且17测试不覆盖设备热路径函数→静默功能断裂。每个device模块完成后须**手工grep核实跨模块运行期依赖真存在**(本次已核 parseCsv@membership/rpNormGroupId@tiers + 全装配体uniq -d查重复定义,doWarn/dispatchAction是有意arity重载非误引入)。
  - **当前装配体**:check.sh GREEN 18/18,去final全文解析EXIT=0(4133行),Security干净。
  - **下一步 T8(onLoad合一+seam收口)**:GA端口接线(makeAndroidStorage/Host/Clock)+l3FlushLoop+RP worker tick启动+旧毫秒键清理+冷启动日志; **接 commands 占位 `_cmdGroupEnabled`↔hooks isGroupEnabled 同源**; **RP `rpOnClickSendBtn` 接进真实 `onClickSendBtn` 双入口合一(撞名收口, 同认 GA「群管」+ RP「红包统计设置」等)**; rpEnableGroup/rpIsGroupEnabled 与 GA enable/isGroupEnabled 同源收口扫一遍。然后 T9 验收(Santa双审热路径合一)。再 **P3-c 灰度真机E2E**(GroupAdminPlus只对1测试群+旧RP关该群破死结,scrcpy用户在场,Santa双审)→全绿后跑P2-b cutover全量切。**T8仍是长阶段,接力时先 bootstrap + 读本快照 + 读 .harness/current-plan.md**。
    - **★挂起需求(2026-06-10, 见下方"挂起需求"节)在 T9 前或 T8 期间一并落地**:发包人豁免(伸手党+@条) + 伸手党警告文案重写(含时间/红包标识)。触及 T6 send + T7 命令 + device_recv/redpacket_app, 实现时需用户拍板文案模板 + 确认是否从统计排除发包人。
    - **★T6给T7的盯点**: hbTierAndSend用cfgCustomRulePrefix(键rp_custom_rule_prefix_<rpNormGroupId>)/cfgRulePrefix/cfgAtLimit/cfgMsgGap/cfgTwoMsgGap全在redpacket_send.bsh已定义, T7设置UI写这些键须同源(尤其定制群规前缀键用rpNormGroupId=trim, 与读侧一致)。
  - **🔄 T8 进行中(扩容版, 用户 2026-06-10 拍板全范围: GA 动作 do* 全套并入 T8)**: onLoad 合一 + seam 收口 + GA 动作层全套。**探查坐实**: 合并体 GA 侧三类 seam 被调用但 merged 从无定义(解析门只查语法掩盖断裂)—— ①启用子系统(getEnabledGroups/isGroupEnabled+ENABLED_CACHE/enableGroupWithBaseline/disableGroup) ②L3基线助手(initFirstSeenBaseline/l3MigrateCsvToDb/l3Count·Clear·UpsertFirstSeen) ③GA动作LEAF全套(~30 do*+makeAndroidLeaf, 现全是 _mkNoopLeaf 空占位→踢/拉黑/名单/潜水/管理员管理全静默空操作, 仅doWarn系列真)。RP收口缺口: onLoad没启rpWorkerTick/没建rp_record表。onClickSendBtn撞名(GA dialogs.bsh:62 vs RP commands.bsh:494 rpOnClickSendBtn)未合一。拆 W0-W8(详见 .harness/current-plan.md + iteration-spec.json)。
    - **✅ W0-W4 完成(2026-06-10, 纯本地零真机; spec W 编号 = iteration-spec.json tasks)**: W0 磁盘瞬态自愈+build冒烟; W1 盘点(线上GA共~890行逐字移植, HOST需补6端口getGroupMemberList/getGroupList/getTargetTalker/getTopActivity/getLoginAlias/delChatroomMember 全真机缝, R5(e)旧毫秒键: GA onLoad无清理N/A而**RP onLoad@449有3行 rp_*_ms 清空须保留**); **W2(commit b29ca00+b906d5c)** 启用子系统 app/enable_state.bsh(ZERO DIFF vs线上@1660-1725, ENABLED_CACHE防FUSE) + L3基线 app/speak_baseline.bsh + domain/csv_time_util.bsh + speak_store加speakCount/Clear/CountGroup/FirstSeenSet + HOST.getGroupMemberList端口(coalesce防NULL, L2 13断言red→green); **W3 do*组A 查询/列表(commit 70ffe81)** 新 app/ga_actions_query.bsh(showHelp/showBlacklist/showAdmins/showWarnList/showProtected/showWhitelist/showGlobalBlacklist/showGlobalWhitelist/doWarnDetail/doCheckBotPriv/logEnabledGroupNames/_logNativeRoleProbe/groupStatus[线上内联@2187包成独立函数]+safeGroupDebug/logGroupEnabledDebug) + admin_exp时效套(membership: setAdminExpiry/clearAdminExpiry/adminExpiryLabel/purgeExpiredAdmins+_parseAdminExp/_saveAdminExp/getAdminExpiry) + 补读助手getBlacklist/getGlobalBlacklist/getProtected/setAdmins/setBotPriv; **W4 do*组B 名单写(commit ef72768)** 新 app/ga_actions_write.bsh(21 do*: doWhitelist/doUnwhitelist/doAddGlobalBlacklist/doDelGlobalBlacklist/doAddGlobalWhitelist/doDelGlobalWhitelist/doAddBlacklistByAt/doRemoveBlacklist+parseBlacklistIndexes/doUnban/doWarnClearAll/doStrictMode/doSetWarnKick/doProtect/doUnprotect/doAddAdmin[2参→3参重载]/doDelAdmin/doAddOwner/doDelOwner/doRegisterNativeOwner/doRemoveNativeOwner) + 补setter(membership: setBlacklist/setGlobalBlacklist/setWhitelist/setGlobalWhitelist/setProtected/setStrictMode/isStrictMode); doBan 前向引用W5。**键同源直查6对(bl_/bl_global/wt_/wt_global/protected_/strict_mode) setter==getter逐字一致。当前 build 7866行, check.sh 22/22, Director独立gate全PASS(uniq-d仅预存重载+合法doAddAdmin重载, Security 0, 抽样doWarnDetail/doStrictMode逐字IDENTICAL)。**
    - **✅ W5 do*组C 破坏性完成(2026-06-10, commit 2873e87, 零真机)** 新 app/ga_actions_destructive.bsh(7函数: warnNoBotPriv/recordFirstSeen/parseIndexRange/doShowInactive/doKickInactive/doKick/doBan)。**端口归一后逐字 IDENTICAL**(Director独立diff + Santa双审A逻辑/B红线 各 READY 0 CRITICAL)。**范围收窄坐实**: ①enableGroup/disableGroup/enableGroupWithBaseline 已在W2(merged 3093-3131)**不重复移植**; ②线上 l3QueryLastSpeak/l3QueryFirstSeen(android.database直查)merged无→唯一结构性改写=用已有 speakSnapshotAll(STORAGE端口)拆分ls/fs(只收非null列值, coldStart=fs.isEmpty()不变, 语义等价); ③缺的 warnNoBotPriv/recordFirstSeen 随W5补; ④doShowInactive 不用lurkClassify(内联自己逻辑)。LURK_NEWMEMBER_EXEMPT_MS@561(无类型赋值=86400000L)。**@所有人 守卫在路由层(out 5683-5686, 踢/请分支)先return, 到不了doKick(v1.17.2逐字); doKick/doBan/doKickInactive 当前由 LEAF 空桩占位(out 5422/5433/5434), W6 接桥时勿绕过各函数内置守卫=安全方向, 本期零活踢人路径。** build 8171行, check.sh 22/22, col-0零新撞名, 依赖闭合17符号全1定义, Security 0。
    - **✅ W6 收口片完成(2026-06-10, commit c927a71, 零真机)** 破坏性 do* 路径首次接活(LEAF.kick→doKick)。改 hooks.bsh/commands.bsh/dialogs.bsh/redpacket_send.bsh/commands_unit.bsh。(A) **makeAndroidLeaf() 桥**(hooks.bsh, 仿_mkNoopLeaf 38方法, 每个转发到真实do*/show*): **bsh 自递归陷阱坐实**——对象方法内裸调同名全局函数(showHelp/groupStatus/disableGroup等)解析到自身→StackOverflow真机崩, 全部 `global.` 限定规避(Santa B 核实漏一个=活体崩, 零漏)。3内联体 enableGroup/disableGroup/banHint 逐字搬线上GA@2166-2185/@2525。桥只转发, 守卫(canActOn/@所有人/botPriv/降级)全在do*内部不绕过。(B) **onLoad RP收口**: GA原5块一字未动, 端口块加 LEAF=makeAndroidLeaf()+STORAGE.exec(RP_CREATE_SQL)(rp_record进groupadmin.db同库), 末尾独立try/catch追加RP块逐字移植线上RP@446-479(清旧ms键/Activity.onResume挂钩/起rpWorkerTick/巨型版本hbBgLog)。**`if(HOOKED)return`→`if(!HOOKED){}`** 合并控制流适配(语义=只挂一次, RP块末尾独立try/catch绝不连坐GA初始化)。(C) **rpDailyMigrateOnce 补全**(redpacket_send.bsh, 消除装配体未定义符号孤儿调用): 逐字线上RP@295, getEnabledGroups→**rpGetEnabledGroups**/normGroupId→**rpNormGroupId**(RP专属符号, 键名保真红线读rp_enabled_groups非GA enabled_groups)+新增 K_DAILY_MIGRATED="rp_daily_migrated"。(D) **onClickSendBtn合一**: dialogs.bsh GA版改名gaOnClickSendBtn(体未动), hooks.bsh统一入口GA先RP后true短路。(E) **_cmdGroupEnabled同源**: →return isGroupEnabled(带TTL60s ENABLED_CACHE真闸门, 热路径同源无C-PERF回归)。Gate: build 8338行+解析门EXIT=0+check 22/22+col-0零新撞名+依赖闭合(38桥目标全定义/孤儿消除)+Security 0。**Santa双审(Layer4): A逐字READY+B破坏红线READY 各0 CRITICAL→NICE**。
    - **✅ W7 VERIFY 完成(2026-06-10)**: 整合复跑 build 8338+解析门 EXIT=0+check 22/22+col-0 uniq-d 仅8既有合法重载零新撞名+依赖闭合(38桥目标 missing=0)+键同源(K_DAILY_MIGRATED=线上@295, RP读rp_enabled_groups)+Security(merged 0泄漏)。全PASS READY。
    - **✅ W8 REVIEW 完成(2026-06-10, commit ec8c1cb)**: 独立 Code Reviewer 整合体端到端最终审(GA命令链贯通/LEAF运行期切生产/onLoad共存无连坐/GA-RP键表零交集/热路径无回归/onClickSendBtn单一入口)。**抓出1个逃逸孤儿 CRITICAL 并修复**: GA dialog 颜色/圆角 helper `_gaC`(调37)/`_gaRound`(调25)早期移植 GA dialog 时漏带, merged 全模块无定义→运行期开"群管"配置面板 NoSuchMethod 崩; **解析门只查语法+22单测无 GA dialog UI 覆盖 → 双重逃逸**(佐证 RP 对等 _rpC/_rpRound 已定义 GA 独缺)。逐字补回线上GA@2009-2015。修后 build 8350/check 22/22/孤儿清零。**→ T8 整合体 READY。**
    - **🎯 T8(W1-W8)全部完成。整合体: build 8350行, check.sh 22/22, 装配体解析门 EXIT=0, GA+RP 单文件合一可跑(零真机本地准出上限达成)。** commits: W6 c927a71 / 记忆992c270 / bsh递归记忆1579977 / W8修复ec8c1cb。
    - **✅ T9 验收完成(2026-06-10, 三审 0 CRITICAL → NICE, 零真机)**: 复跑门禁基线无漂移(build 8350/解析门 EXIT=0/check 22/22, 提交体仅 .harness 状态变)。**三独立 reviewer 实例并发对抗审**: ① Santa-A 热路径逐字保真(C-PERF-01/03: 热路径零O(N)/getString全走缓存/重活在worker/RP检测先于GA非文本早退/合一体逐字)= READY; ② Santa-B 破坏性降级红线(LEAF桥不绕do*守卫/global.限定齐全无自递归/@所有人守卫/伸手党无快照·无此人·查询<0=SKIP绝不误踢/踢人降级方向宁可不踢/事件时刻快照防抢后洗白)= READY; ③ Spec-C 整合体规格+同源(键名RP读rp_enabled_groups·GA读enabled_groups不混/onLoad RP块独立try-catch不连坐/onClickSendBtn GA先RP后/rpDailyMigrateOnce逐字/依赖闭合无孤儿_gaC·_gaRound已在/命令路由无落空)= READY。**Director 无author-bias独立复核两个 MAJOR 均属实**: (a) Santa-A 埋点 MAJOR——merged onHandleMsg 累加到 RP 的 PERF_SUM_NS/MAX, GA 专有 PERF_OHM_*/PERF_RS_*/PERF_CMD/PERF_SEG1-5 全缺失(grep merged=0, 线上GA@63-110有)。**裁决=可观测性缩减非功能问题, 不阻 T9**: PERF_OHM≡新PERF_SUM 同义、PERF_SEG1-5 是GA旧路径分段合并后不适用、PERF_CMD被RP的A/B分类覆盖; **唯一真损失=PERF_RS_*(recordSpeak热路径计时, C-PERF-01/VH-01 关注点)** → 列 P3-c 读perf.log时可选增强(net-new 2调用点非逐字移植, 本期不补避免反向破坏逐字保真+动热路径)。(b) Santa-B 快照清理——经查 FREELOADER_SNAP.remove 在 device_recv.bsh 5路径全覆盖(@307 backstop/@350·382·435 终结兜底/@427 判定读完即清), since闸门跳过落回hbDetailExtract仍命中@427, 无孤儿泄漏, 降MINOR正确。其余 MINOR(反射取值时机提前语义等价/新人豁免有意提升/rpCurrentTalker用GA normGroupId标准群ID等价)均非actionable。**🎯 T8+T9 收官: GA+RP 单文件合一体本地准出上限全部达成(逐字保真+解析门+check全绿+三审0 CRITICAL), 零真机。**
    - **✅ P3-c 前置补丁完成(2026-06-10, commit 5fc14b6, 用户3条反馈, 行为变更非逐字保真, 纯本地零真机, Santa双审NICE)**: ① **发包人豁免**(用户定:写库照写,只豁免判定+@条)——伸手党判定 exSet 加 job[JOB_SENDER](device_recv.bsh:404)、@条 hbTierAndSend 透传 senderWxid 跳过(redpacket_send.bsh:234/327, 5参签名+旧2/3/4参入口保留), rpRecordStats写库未动。② **伸手党文案**(用户定例子)——reason=`{HH:mm}红包 · {X}分钟未发言领取`(device_recv.bsh:411, SimpleDateFormat+整段try/catch回退原串), 不叠"警告"(doWarn前缀已含⚠️警告N/10)。③ **共通发言基线(新约束 C-ARCH-04)**——用户洞察"基线=共通基础设施,不被功能开关左右;开关只控逻辑时间窗+功能"。落地范围=**全解耦**(用户拍板"和全解耦类似,但基于基线计算的潜水/伸手党各按不同保护时间"):C1 onLoad基线群集=getEnabledGroups()∪rpGetEnabledGroups()(任一功能启用即建first_seen, 键各读各表); C2 热路径recordSpeak**上提为onHandleMsgBody单一共通点**(hooks.bsh:341-349, 在rpConsumed return后/isText前, `isGroupEnabled||rpIsGroupEnabled`两次TTL60s缓存=O(1), 撤routeCommand@224+非文本分支@348两处重复采集, 红包发包人/非文本sender仍记一次); C3 **移除伸手党since闸门**(redpacket_app.bsh within_activation_grace/no_since_anchor整段+写点commands.bsh:707/dialogs.bsh:1483+死键K_FREELOADER_SINCE_PREFIX/rpFreeloaderSince全删), 新人保护**完全交first_seen的FL_EXEMPT_NEW**(freeloader.bsh:46, 用FREELOADER_NEWMEMBER_EXEMPT_MS); C4 **红线:潜水LURK_(speak.bsh:17)/伸手党FREELOADER_(freeloader.bsh:21)两豁免常量独立不合并**。**已知权衡**: 移除since宽限后"刚首次启用任一功能"预热窗内,老成员若last_speak仍空且非新人→会被判伸手党(原grace护此);共通追踪(C2任一开就记)使数据通常已热,降级红线(null/无此人=SKIP/新人豁免)Santa-B复核保持"宁可不踢"。Gate: check 23/23 GREEN+解析EXIT=0+build 8351+Director独立复跑全PASS; **Santa双审(A热路径+采集+文案 / B伸手党红线+豁免+保护时间独立)各READY 0 CRITICAL→NICE**。关联 [[config-key-written-on-transition-upgrade-gap]](since闸门正是这类"开关锚点"反模式, C-ARCH-04已立规根治)。
    - **✅ P3-c Phase 1 完成(2026-06-11, commit 55f06a5, HOST/_msg 真机反射缝逐字移植, 零真机, Santa双审NICE)**: 让合并插件在真机能动起来的全部反射缝接活。**摸底坐实占位缝**: host_android.bsh 仅 3 真占位(detectNativeOwner→null/lookupName→wxid/tryKickSilent→false; getGroupMemberList/sendQuote/sendText/getString等已转发真内置), hooks.bsh 10个 _msg* 全占位(return false/null/0); 红包UI反射(hbFindReceiverList/hbExtractTriple/hbOnActivityResume)+sendQuote已 T4/T5/T6 实现; getGroupList/getTopActivity/getFriend*/delChatroomMember 是 WAuxiliary 内置已被既有模块直调。**移植**: ① **10×_msg* 缝**(hooks.bsh:225-238)占位→真实反射+try/catch兜默认(isChatroom/isText/getContent/getTalker/getSendTalker/getAtUserList 各GA+RP同款; getType/getMsgId 逐字RP@1237/1262的`((Number)x).intValue()/longValue()`; getQuoteMsg().getSendTalker(); `<title>(.*?)</title>`DOTALL抽group(1)原始内文, split/qDays在commands.bsh:391消费侧)。引用字段提取**下沉!isTextMsg分支**(hooks.bsh:367-374, Santa-A MINOR: 普通文本零getQuoteMsg/正则成本, 逐字还原线上C-PERF-01"普通文本零额外成本"; 已查routeCommand仅!isText分支消费这两参)。② **host_android.bsh**: lookupName(GA@1917-1938)/tryKickSilent(GA@691-701, 破坏性真踢)/isPermissionError(GA@677-687) 逐字, Director程序化diff**字节相同**(模缩进+groupId→grp)。③ **新模块 native_owner_reflect.bsh**: _detectNativeOwner 整条反射链**29函数**(GA@792-880;916-1381)byte-identical, callee闭合零孤儿(排除 _addRoleValue/_addRoleList/_probe* 确不在闭合), 入manifest.prod.txt(host_android后), host detectNativeOwner方法体→`return _detectNativeOwner(grp)`(下划线不自递归)。**落点**: detectNativeOwner用于membership.bsh:77 canActOn原生群主保护(破坏降级红线), null时退回手动nat_owner_键兜底=降级安全。Gate: build 8991行/解析EXIT=0/check 23/23 GREEN/Director全字节diff通过/Security零泄漏。**Santa双审(Layer4)**: A(热路径逐字+C-PERF-01)READY 0C0Maj1Min(quoteTitle下沉已修) + B(破坏tryKickSilent守卫无漂移+detectNativeOwner降级安全+29函数零孤儿零自递归)READY 0C0M → NICE。
    - **⏭ 下一步 P3-c Phase 2 真机灰度E2E(⚠️不可逆, 需用户在场scrcpy + 另开PLAN逐条确认)**: 不可逆动作清单(每条要拍板)= ① adb push out/main.java→真机新插件GroupAdminPlus; ② config策略(union旧两插件键 vs 空config起); ③ 启用范围(仅1测试群:群管+红包统计); ④ 关该群旧RP破红包双跑。E2E矩阵: 消息收发不卡/群命令/红包检测/伸手党(测试群临时开)/发包人豁免/新文案/**共通基线(RP-only群也记speak, 验C-ARCH-04)**/移除since闸门后伸手党判定(尤其预热窗权衡)/logcat无异常/perf.log热路径数字。然后Santa双审真机证据→P2-b cutover全量切(再另开PLAN)。P3-c读perf.log时可选补PERF_RS_*。**挂起需求已清零**(发包人豁免+文案已落地)。
    - **🔄 P3-c Phase 2 真机灰度进行中(2026-06-11)**: GroupAdminPlus 已 push 真机, 群B群(+另2群 GROUP_A_ID/GROUP_C_ID)启用群管+红包, 旧插件全关。**真机暴露并修 2 bug**: ① **日志路径漏隔离**(commit b7e12d0, VH-07): HB_LOG/PERF_LOG 仍指旧 `Plugin/RedPacketStats/` → 改 GroupAdminPlus/(C-ARCH-06: 隔离须重定向 db+日志全部路径常量)。② **伸手党新人豁免期 24h→winMs**(commit c28da75, VH-08/C-ARCH-07): 真机诊断"伸手党没生效" = 合成基线(enableGroupWithBaseline 全员 first_seen=now, 拉db实证群B群482人全落今早)撞 24h 潜水豁免 → 启用后24h全员静默失效; 改 `exemptMs=发言窗口 winMs`(device_recv.bsh: 进群<X分没机会发言→豁免, >X分窗口内没发言→判)。真机复测同群拼手气: `judged=4 warned=3 sent=3 active=1 exemptNew=0 excluded=1`, wn_ 三人各=1 doWarn真落地。check.sh 23/23。**E2E 已过**: 红包检测/领取者反射(3-5/5)/分档/发包人豁免(excluded=1)/新文案/**伸手党警告(warned=3)**。**E2E 待补**: 群命令破坏性踢人实测。部署范式见 [[waux-plugin-root-deploy-pitfalls]](push→su cp→chown u0_a280:media_rw+chmod660+chcon media_rw_data_file:...c24,c257,c512,c768→force-stop重载)。
    - **★新挂起需求(2026-06-11): 群管+红包 设置界面合并重建(大改, PLAN 已出未执行, 见 .harness/current-plan.md)**: 用户要把两个设置界面合并成一个——插件列表「设置」按钮(openSettings)→先弹选群(复用 rpPickTarget 的 getGroupList 后台加载范式=进退群版群列表读取)→配该群两功能; 群内打「群管」**直进当前群**合并页(跳过选群); 最外层加**群管/红包各一个全局开关**(语义=**一票否决**: 全局关→该功能所有群停, 全局开→看单群 enabled_groups/rp_enabled_groups; 键空=ON 防升级缺口)。热路径全局门挂 `isGroupEnabled`/`rpIsGroupEnabled` 顶部, 必须走缓存保 O(1)(C-PERF-01)。**不重写两棵现有菜单树**(showGroupConfigDialog GA / showConfigDialog RP 原样作功能子页, 上加合并父页)。7任务 T1-T7 拆解在 current-plan.md, 等用户确认后 EXECUTE(末步真机 force-stop + Santa 双审热路径)。
    - **✅ UI 合并重建 EXECUTE 完成(2026-06-11)**: T1-T7 全做完。**T1/T2 全局门**: enable_state.bsh 加 `gaGlobalOn()`/`setGaGlobal()`(键 `ga_global_on`, GA_GLOBAL_CACHE 60s TTL) + isGroupEnabled 顶部 `if(!gaGlobalOn())return false` 一票否决; redpacket_detect.bsh 对称 `rpGlobalOn()`/`setRpGlobal()`(键 `rp_global_on`, RP_GLOBAL_CACHE) + rpIsGroupEnabled 顶部同款。**空键=开**(`!"0".equals(get(k,"1"))`)防升级缺口; 异常降级默认开; 热路径 O(1) 走缓存(C-PERF-01)。**T3-T5 dialogs.bsh** 加 `showMergedHome()`(两全局 toggle + 选群入口 + 红包全局默认) / `pickGroupForConfig()`(复用 rpPickTarget 后台 getGroupList→单选范式) / `showMergedGroupConfig()`(本群 GA/RP 启用态【直读 enabled_groups/rp_enabled_groups 忽略全局门, 诚实反映】+ 两本群开关 + 两详细入口→原 showGroupConfigDialog/showConfigDialog 不重写); 重接 `openSettings()`→showMergedHome、`gaOnClickSendBtn`「群管」→showMergedGroupConfig(talker, 直进当前群)。**T6** check.sh GREEN **24/24**(新增 tests/global_gate_unit.bsh 10 断言钉死 空键=开/一票否决/即时生效), 解析 EXIT=0, build 9395 行。**T7** 真机部署(main.java 9395 行 push→chcon, 备份 main.java.bak-before-ui-merge-20260611)+force-stop, **真机 09:06:39 干净加载**(onLoad/RP worker/baseline 正常, 无 NoSuchMethod/解析错误); **Santa 双独立审 A(正确性/热路径/升级安全)+B(BeanShell陷阱/UI接线/对称) 各 PASS→NICE**。**待用户真机手测**: 选群入口/群内「群管」直进/全局一票否决热路径生效/升级安全键空默认开。
    - **✅ UI 合并 收尾(2026-06-11)**: ① **F1(Santa-B 复审发现)**: `pickGroupForConfig` 选群列表改用 ArrayList 过滤掉 `getRoomId()` 反射失败的空/非@chatroom 条目(原定长数组会把空 id 暴露给用户, 选中后弹「无效群」), 全空兜底 toast; check.sh 24/24 GREEN + Santa Fix-Cycle 复审 NICE。真机重部署 main.java **9400 行**(备份 main.java.bak-before-F1-20260611)+force-stop, **10:25:45 干净重载**(onLoad/RP worker/baseline 正常, logcat 零异常), config 两全局键不存在实证升级安全默认 ON。② **info.prop 说明**: 加 description(面向新用户使用指导: 上手三步/快捷入口/总开关/功能简介) + version 正式化 `1.0.0-merged(...)`→`1.0.0`, 已部署设备。③ **旧插件移除(设备侧)**: 设备 `WAuxiliary/Plugin/{GroupAdmin,RedPacketStats}` 移出到 `WAuxiliary/Plugin.removed-backup-20260611/`(Plugin/ 外不被扫描=从列表消失, **非硬删可恢复**); 本地仓库 plugins/ 保留。⚠️ **历史数据提示**: 旧 `RedPacketStats/redpacket_stats.db`(139KB 历史红包统计)灰度时**未迁入** GroupAdminPlus(合并体 RP 数据统一写 `groupadmin.db`, 独立 redpacket_stats.db 是空壳), 备份区可恢复; 如需历史红包统计需另做迁移。
    - **★新功能设计就绪: 排行榜/趣味群英榜(2026-06-11, 嗅探已坐实, 待 PLAN 开发)**: 用户要总发言榜+各种"之最"(话痨/水王/红包王/手气王/拍一拍狂魔/熬夜冠军…)趣味文案手动出榜。**数据建模(用户拍板)**: A=逐条原始事件 msg_event(grp,wxid,type,ts,val) 唯一真相全量保留; B=榜单/月度 rank_rollup 全从 A 派生; 文本记字数(复用 val 列, 热路径 .length() 白送); 量级 ~1GB/年需 ym 分区+rollup+归档+体积看门狗。**真机嗅探判据已坐实**(临时只读探针 PROBE 已还原+清日志): 拍一拍=content含`拍了拍`形如`"${A}" 拍了拍 "${B}"`(两wxid); 转账=content含`<wcpayinfo>`(feedesc金额/payer/receiver/transferid去重/paysubtype 1发起3收款, **每笔2条消息同transferid**, 群转账成立); 红包=`<nativeurl>`现成。**两坑**: `msg.getType()`返回大数不可靠→检测一律走content标签; "转账"二字误命中聊天→转账必用`<wcpayinfo>`标签。完整设计见 `docs/superpowers/specs/2026-06-11-排行榜-ranking-design.md`。关联 [[waux-getstring-fuse-io-cache-hotpath]](热路径采集纪律) [[concat-port-needs-callee-closure-scan]]。
    - **🔄 24h 稳定性/性能监测进行中(2026-06-11 09:18 起, 后台脱离)**: `scripts/monitor/stability-24h.sh`(nohup+disown 真脱离, 每小时跑 `stability-cycle.sh` 一次共24次)→报告 `scripts/monitor/reports/stability-20260611-091804.log`。每小时自动采集+判定: 微信存活/onHandleMsg 热路径 norm_avg(基线 ~21.5ms)·norm_max·ohm_max/spike/plugin.log异常增量/伸手党summary增量, verdict=OK|WARN|ALERT。**关键修过的坑**: `su -c '...'` 外层单引号与 grep 模式内引号/管道冲突→改「cat 日志回 Mac 本地 grep」。**接力时读该报告文件给汇总**(不靠 session 常开, CLAUDE.md 铁律)。需 Mac 不休眠 + 手机 USB 连着 24h。

## 挂起需求(用户 2026-06-10 记录)——✅ 已全部落地(commit 5fc14b6, 见上 P3-c前置补丁)
> 发包人豁免(写库照写只豁免判定+@条)+ 伸手党文案重写均已实现+Santa双审NICE。下方原始记录保留备查。
**F1 原话**:
1. 「伸手党治理和红包提醒，发包的人不需要提醒」
2. 「伸手党警告文案，简短些，但要说清是什么时间哪个红包，X分钟未发言领红包，所以警告」

**F2 分类 = 工具层(插件逻辑)。触及已做的 T6 发送 + 伸手党判定, 在合并工程内落地(非旧线上插件)。**

**需求 1 — 发包人豁免(伸手党 + 红包提醒 @条 都不针对发包人本人)**:
- 触点 a(红包提醒 @条):`redpacket_send.bsh hbTierAndSend` 分档 @ 时, 把发包人 wxid(`job[JOB_SENDER]`)排除, 不 @ 发包人(类比排除名单 excl 的剔除位 @hbTierAndSend 分档循环)。
- 触点 b(伸手党判定):`device_recv.bsh hbDetailExtract` 组 wxids / `redpacket_app.freeloaderRunWithSnapshot` 判定时, 把发包人 wxid 排除, 绝不把发包人判成伸手党。发包人 wxid 需从 device_recv 透传进判定(当前 freeloaderRunWithSnapshot 入参只有 receivers wxids + excludeSet, 可把 senderWxid 加进 excludeSet 或单列豁免参)。
- ⚠️ 待澄清(实现时定):是否也从**统计写库 qualifiers/qualified_count** 排除发包人(还是只豁免 @/警告)?正常微信发包人不抢自己的包(不会出现在 receivers), 但用户明确提了 → 实现时确认是"防御性排除"还是确有发包人进 receivers 的包类型。
- 落地任务:可作 T6 补丁 或 并入 T9 前的收尾(T6 send 模块 + device_recv/redpacket_app 三处)。

**需求 2 — 伸手党警告文案重写(简短 + 信息全)**:
- 现状:`device_recv.bsh` reason = `win + "分钟内未发言"`, doWarn auto 拼 ` · 理由`(线上 @2794 "警告 {N}分钟内未发言"); 通知无红包时间/标识。
- 目标:简短但说清 **什么时间 + 哪个红包 + X分钟未发言领红包 + 所以警告**。需把红包信息(检测时刻 `packetMs`→HH:mm / 红包标题或时间标识)带进 reason。
- 触点:reason 串生成处(device_recv 调 freeloaderRunWithSnapshot 前构造 reason); 需把 packetMs(已有)格式化 HH:mm + 可选 title(job[JOB_TITLE], 写库已用)拼进文案。doWarn 拼接格式可能要调(理由含多信息)。
- ⚠️ 待澄清(实现时定):文案确切模板(例:`{HH:mm}红包·{X}分钟未发言领取·警告`)请用户最终拍板;"哪个红包"用时间还是标题标识。
- 落地任务:同需求 1, 一并做。

## 后续项(伸手党收尾时浮现)——均已完成 ✅
1. ✅ **#潜水 新成员豁免改固定 1 天**:GA v1.20.0 已做(`LURK_NEWMEMBER_EXEMPT_MS`)。
2. ✅ **GroupAdmin 主动补全员 first_seen 基线**:GA v1.20.0 已做(onLoad 对所有已启用群补 + coalesce 防 NULL)。

## 工作目录已迁 Dropbox
工作目录 = `~/Dropbox/ops-work/android-ops`（多 Mac 经 Dropbox 同步）。新 session 先 `bash scripts/bootstrap.sh`（自识别机器/adb/scrcpy/手机/root/版本 + 本机记忆为空时从 `docs/memory-snapshot/` 自动 seed）。详见 `docs/ONBOARDING.md`。**写了新记忆后要 `cp ~/.claude/.../memory/*.md docs/memory-snapshot/` 同步回快照**，否则别的机器接不到。

## 当前部署版本（真机已上）
- **GroupAdmin v1.20.0**：在 v1.16.3(onLoad 冷启动健壮化 C-PLUGIN-05/VH-02)之上:
  - v1.17.0 **管理员时效**:加管理可选永久/N天,并行键 `admin_exp_<gid>`(未设=永久,向后兼容);到期惰性清除 `purgeExpiredAdmins`(管理函数入口,不进热路径);@命令 `@TA 管理员 [N]` + Dialog 天数双入口;Dialog 加删静默、群@命令保留群消息。
  - v1.17.1 列表/群消息文案「**有效期至** yyyy-MM-dd」(adminExpiryLabel 同源)。
  - v1.17.2 **@所有人(notify@all)不能踢** — @路径踢/请前置守卫 `_isAtAll`(标记真机待最终确认)。
  - **v1.18.0 潜水误判修复(2026-06-08)**:非文本消息(图片/表情/语音/红包/文件)`getContent()` 空, 原在 onHandleMsgBody 入口 isText 闸门早退、走不到 ~2188 行 recordSpeak → 只发非文本的人 last_speak 永空被 `#潜水` 误判潜水。修复=早退点前先记一次发言活动(只调 talker/sendTalker/isGroupEnabled缓存/recordSpeak O(1), 不调 getQuoteMsg, try/catch, 与主路径互斥)。真机实测纯图片使 owner last_speak 推进。**冒泡=任意消息类型**。详见 SPEC §3/§6.10。
  - **全群基线重置(2026-06-08 17:19)**:应用户要求"从今天起算", 停微信→`UPDATE speak SET first_seen=now`(全 8 群所有行, **last_speak 保留**)→推回→重启验证。真机留底 `groupadmin.db.before-baseline-reset-20260608`。**预期**:之后 N 天 `#潜水 N` 基本空(全员新成员豁免期), N 天后从今天起没冒泡的人才上榜。
  - **v1.19.0 警告带理由(2026-06-09)**:`@TA 警告 <理由>` 解析(末token非动作词但前一token="警告"→动作=警告+理由), `doWarn` 双签名(旧3参转调4参), 通知拼 ` · 理由`; **自动来源(理由非空)对豁免对象静默跳过**(不发报错)。手动 `@TA 警告` 不变。为伸手党治理铺路。
  - **v1.19.1 自动来源豁免管理员(Santa 修复)**:`canActOn(owner→admin)=true` 会让自动警告误踢潜水管理员; doWarn auto 分支前置 `if(auto&&(isAdminInGroup||isOwner||isNativeOwner))return;`。
  - **v1.19.2 [严重] recordSpeak last_speak 永卡 NULL(2026-06-09)**:l3Flush 的 UPSERT `last_speak=max(last_speak,excluded)`,**SQLite 标量 `max(NULL,x)=NULL`** → 先被建基线(last_speak=NULL)再发言的成员永远卡 NULL。**影响发言统计/#潜水(活跃者误判从未发言)/伸手党 全失真**(该群 491 人里 404 NULL,很多其实在发言)。修:`max(coalesce(last_speak,excluded),excluded)`(对齐 first_seen 那行本就有的 coalesce)。真机验证:长期 NULL 的活跃成员部署后再发言 last_speak 立即更新。**教训:SQLite UPSERT 里凡 max/min 旧列可能为 NULL,必须 coalesce 兜底**。注:此前 06-08 全群基线重置(first_seen=now)放大了此 bug(全员有 NULL 行)。修复后大家发言逐步自愈。
  - **v1.20.0 #潜水豁免+基线(2026-06-09, commit 95e012b)**:(1) **#潜水 新成员豁免改固定 1 天**(`LURK_NEWMEMBER_EXEMPT_MS`, 与潜水窗口 days 解耦):豁免 `fst>now-1天`、活跃仍 `lst>now-days天`;冷启动文案改"豁免1天"。语义=`#潜水 7` 列出"进群>1天 且 7天没发言"的人(原"进群>7天"), **名单更全**, 仍手动列表不自动踢。(2) **onLoad 对所有已启用群补 first_seen 基线**(去 `l3CountFirstSeen>0` skip, 幂等只补缺失), 堵"入群从不发言者无 speak 行→红包伸手党 rpQueryLastSpeak 返-1 漏检"窄缝(用户洞见)。(3) **`l3UpsertFirstSeen` coalesce 防 NULL**:`min(first_seen,excluded)`→`min(coalesce(...),excluded)`(对齐 v1.19.2 last_speak 教训)。验证:bsh EXIT=0 + 真机干净加载 baseline check「补基线0 新增0」(全员已有基线) + **受控 DB 实测 coalesce**(NULL→now/更早值保留/无行插入, 老 UPSERT 对比留 NULL 坐实) + Santa 双审(A逻辑PASS; B规格抓到 onLoad catch 块 stale v1.17.0 版本串→修→PASS)。#潜水 名单行为(T1/T2)需手动 scrcpy。设备留底 main.java.bak-before-v1200-20260609。**两个后续(#潜水改1天 + GA补基线)至此都做完**。
  - GroupAdmin 重启即加载。
- **RedPacketStats v1.11.4**：v1.6.4 基础上累积 包类型(§22)/设置页拆分(§23)/转发对象按群(§24)/重试3(§4)/伸手党治理(§25, v1.11.3 收尾 + v1.11.4 升级缺口自愈)。
  - 包类型 普通/定制(v1.7.x):定制开关(仅 Dialog)+关键字(**包含匹配**)+定制档表;worker 判定零热路径;定制第一条用定制前缀、第二条「【查包】定制包」。
  - 设置页(v1.8.x):主菜单 + 二级子页(showPageBasic/NormalTiers/Custom/Delay/Exclude/**Forward**);menuHolder 修关闭堆叠;排除名单显示「昵称（…尾4）」。
  - **转发对象按群**(v1.9.x §24):`rp_export_target_<gid>`/name(未设回退全局默认);后台 `getFriendList()`/`getGroupList()` + Handler 主线程单选弹框(防 ANR);设置后向目标发通知(只讲当前群);发送按群路由(rpBuildGroupedMsgs 带 grp);**每日定时开关+推送时间(全局)迁入「📮 转发对象」子页**,基础页只留本群启用。真机验收过(含选好友 getFriendList)。
  - **重试3(v1.10.0 §4)**:红包重试阶梯加末档 `rp_retry3_sec`(默认3600s=1小时), afterCoverNoDetail attempt≥2→≥3, 三段阶梯; cfgRetry3 仅 worker 读。延迟子页/命令(4参)/状态增列重试3。
  - **伸手党治理(v1.11.x §25, 2026-06-09, 默认关 `rp_freeloader_on_<gid>`)**:抢红包者在**红包检测时刻**前 N 分(默认30 `rp_freeloader_win_<gid>`)未在群发言 → 后台 fire-and-forget 线程发 `[AtWx=wxid] 警告 {N}分钟内未发言` 命令, 交群管 v1.19.1 原警告流程执行(同一计数到 wk_ 上限踢)。只读查 groupadmin.db speak.last_speak(rpQueryLastSpeak: -1无行/DB不可用跳过、0从未发言判、>0毫秒)。Santa 修复: 后台线程不占单飞锁/不撞30s看门狗 + 上限 FREELOADER_MAX_WARN=10(超出留痕) + flush 容差60s。安全网宁可漏判; 豁免靠群管 doWarn。只拼手气包、只领完的包。命令 `伸手党 开/关`、`伸手党窗口 N`; Dialog 子页🖐伸手党治理。**⚠️ 端到端启用实测(开开关+已知活跃/潜水各抢一次+管理员豁免)还没做, 用户暂未开启。**
  - **教训:红包领取者反射模型(LuckyMoneyNewReceiveUI 详情)无 per-person 领取时刻字段**(`g` 取不到、无任何时间戳数值字段; 真机 GPROBE 探测确认)。本想用每人领取时刻判定, 退用红包检测时刻(Job 加 JOB_DETECTMS 第10槽承载)。要按人领取时刻得另挖(UI 文本/字符串字段, 更脆弱)。
  - **伸手党 v1.11.3 收尾完成(2026-06-09, commit b747a58, 默认关)**:撤 DRY-RUN/GFDBG 恢复真发送。**豁免模型(用户最终拍板)= 全局开关时刻宽限, 不是每人 first_seen**:新增 per-group 键 `rp_freeloader_since_<gid>`(命令『伸手党 开』/Dialog OFF→ON 写当前 ms, 已 ON 不重置); 判定段闸门 `since<=0`(安全网跳过) 或 红包检测时刻 `packetMs` 在 `since+winMs` 内 → 整包跳过(全员豁免=刚开开关数据预热期); 宽限后**所有领取者(含新进群)按 last_speak 判, 不用 first_seen**(与 #潜水 刻意区分:进群就抢、抢前没发言即判)。复用红包排除名单 `getExcludeList` 豁免领取者。排查确认红包 wxid 与聊天 wxid **一致(无错配)**, 误判根因就是 GA v1.19.2 的 last_speak NULL bug(已修)。验证:bsh 解析 EXIT=0 + 真机干净加载无 eval/parse 异常 + Santa 双独立 Reviewer(逻辑+规格)均 PASS。**⚠️ 真红包行为实测(T1-T5)adb 模拟不了, 需手动 scrcpy(用户在场+单测试群+临时『伸手党 开』); 且 last_speak 仍自愈中(大量 NULL), 30min 宽限盖不住多小时自愈期 → 建议自愈后或仅测试群再开。默认关, 未启用。见 docs/verification-report.md**。设备留底 main.java.bak-before-v1113-20260609。
  - **v1.11.4 伸手党升级缺口自愈(2026-06-09, commit c6de767)**:用户报"刚才几个红包伸手党没发挥作用"。根因(rp.log 实证, 系统化调试):伸手党 v1.11.2 测试时已 `伸手党 开`(10:00 真发 warned=4), v1.11.3 引入「开关打开时刻 since」闸门(只在 OFF→ON 写), 升级时开关带"已开"状态从未经历 OFF→ON → `since=0` → 判定段 `since<=0` 安全网整包跳过 → 静默失效("freeloader skip: no since anchor")。**不是逻辑 bug, 是升级状态缺口**。修:判定段 `since<=0` 由直接跳过改 **lazy-init 补写 since=now 再跳过**(自愈、免手动重切, 下一个包按宽限/判定生效, 仍保守不当轮判)。Santa A 抓到三处 since 写点用裸 groupId、读用 normGroupId(群 ID 无空格不触发但结构性不一致)→ 统一套 normGroupId。**测试配方(避 30min 宽限):测试群 `伸手党窗口 1` → 发抢拼手气包(第1个触发 lazy-init 跳过)→ 等1分钟 → 再发抢(抢前1分钟没发言者被警告)→ 测完 `伸手党窗口 30` 调回**。lazy-init 真触发需真红包, 已部署待用户实测。设备留底 main.java.bak-before-v1114-20260609。**教训见 [[config-key-written-on-transition-upgrade-gap]]**。
  - **v1.11.5 抢后发言洗白漏判修复(2026-06-09 已部署 15:28, commit 待定)**:用户报"河北🐽(qualifiers 里 🐽)抢前没发言为何没警告"。系统化调试(拉 rp.log+config+groupadmin.db speak 行)定位:🐽 抢的是 53740 群 14:30:26 包(检测 14:29:00, 伸手党开 win=10 宽限已过),🐽 上次发言昨晚 20:00(窗口内沉默=伸手党)但**抢完 14:29:14 说了句话**。判定在 RP 延迟开包后 14:30:26 才跑,`rpQueryLastSpeak` 读到 last_speak=14:29:14>thr → 误判活跃漏警告。**根因=判定读"判定时刻"的 last_speak,GA speak 表只存最近一次发言(无历史);判定被开包延迟推后~90s,伸手党抢完说句话就把 last_speak 推过阈值,洗白**。修(SPEC §25.10):红包**检测那一刻** fire-and-forget 线程快照本群 `wxid→last_speak` 存 `FREELOADER_SNAP`(ConcurrentHashMap),判定改**双判据** `freeloaderDecide(curLs,snapLs,packetMs,winMs,graceMs)`:有快照 `spokeInWindow=(curLs∈[thr,packetMs]兜flush延迟)||(snapLs>=thr抓抢前发言)`→活跃否则伸手党(抓抢后洗白);无快照降级回当前值判据(保守不误警告)。热路径只多一次 Thread.start()(cfgFreeloaderOn 移进后台线程,不占消息线程 FUSE)。判定段加**恒定 summary 日志**(judged/warned/active/skip_norow/excluded/snap=ok/fail,修原"warned=0 全静默"盲区,正是排查难的直接原因)。L1 纯函数单测 8 例(`tests/freeloader_judge_test.bsh`,red→green 验过)+ Santa 双审 NICE。设备留底 main.java.bak-before-v1115-20260609。教训见 [[delayed-judgement-needs-event-time-snapshot]]。
  - **⚠️ v1.11.5 仍误判(2026-06-09 真机实证, 用户报"发过言的人被警告"), 已确诊+决定合并后修(P3), 暂不再打补丁**:三个例子三个失败模式、同一病根 —— (1) `packetMs`=红包检测时刻, **重试包冻结在原始发出时刻**之后发言不算; (2) `freeloaderDecide` 的 `curLs<=packetMs` 上界把"红包后还在说话"的活跃成员排除; (3) 快照在检测那刻拍, 但 GA speak 每 30s 才异步 flush → 红包前最后 ~30s 发言还在 GA 内存缓冲没落表, RP 跨插件读不到 → 快照漏掉。**根因三合一: 微信无 per-person 领取时刻 + speak 表只存最新值 + 跨插件读异步表慢一个 flush 周期。靠跨插件读救不了, 合并后同插件 in-process 才能根治。** GA 侧无辜(今天零降级、表新鲜、文本+非文本都记)。**用户决定: 合并后修; 设计+测试用例已完备(见 freeloader 合并后判定重设计文档 + merged/freeloader.bsh 已实现纯判定层)。语义细化(用户拍板): 活跃=`[packetMs-N, packetMs+G]` 内发过言, **G=3s 事后宽限**(评审建议≥10s, 记为已知风险, per-group 可配, 建议生产先大值观察); 从未发言→伸手党但**新人(进群时间<1天)豁免**(D2); 重试包用原始检测时刻(D3); **无快照/失败/无此人=SKIP 绝不误踢**(降级红线)。在合并 P3 落地, 届时把 v1.11.5 的检测时快照+双判据换成"单快照拍在 packetMs+G + in-process flush"。** 在合并修好前 v1.11.5 仍会误判活跃成员(用户知悉, 暂未关开关)。
  - **懒加载;曾因解析失败被自动停用,注意确认开关 ON**。

## 挂起的真机收尾项（adb 输不了中文/红包，需人工在微信里做）
1. RedPacketStats v1.6.4：发个拼手气红包 → 拉 perf.log 确认红包消息热路径从 ~130ms 降到几 ms（这次优化的最终数字还没坐实）。
2. enable/disable 即时生效：`关闭/开启红包统计`、`关闭/开启群管` 切换后下一条消息/红包立即按新状态。
3. 确认 WAuxiliary 里 RedPacketStats 开关为 ON。
4. 专属红包应被 worker 侧跳过(`skip exclusive (worker-side)`)。

## perf 埋点路径（拉数据用）
`/storage/emulated/0/Android/media/com.tencent.mm/WAuxiliary/Plugin/<GroupAdmin|RedPacketStats>/perf.log`（脱敏，只耗时/计数）。GroupAdmin 有 seg1-5 分段；RedPacketStats 有 rp_msg/norm/spike_rp/spike_norm/rp_extract。

## 本机(DJRMBP / macOS 13)环境与设备连接
- macOS 13 是 Homebrew Tier 3(无预编译包),`brew install` 会从源码编译(scrcpy 依赖链编了 ~20min)。adb/scrcpy 已装好(`/usr/local/bin`)。
- **一键连接 + 息屏投屏**:双击桌面 `akita-连接.command`(源 `scripts/akita-connect.command`)——自动起 adb/等授权/`scrcpy -S -w --power-off-on-close`(手机黑屏、电脑窗口实时控制)。adb 输不了中文/红包时用投屏窗口手动操作微信。
- **充电"不支持的充电线"告警 = 插入瞬态,非故障**:30min 连续采样实测 92%→100%、Charging 60/60、零横跳;别再被插线瞬间那条告警带去排查线缆。满电后转 Not charging/Full 是正常涓流。
- **后台 watchdog**:`scripts` 外的临时脚本 `/tmp/akita_watchdog.sh`(每5min 查设备掉线/微信进程/插件log新报错,异常才报)——VH-02 就是它真机抓到的。新 session 要常驻监控需重新拉起。

相关 [[waux-getstring-fuse-io-cache-hotpath]]、[[beanshell-parser-stricter-than-javac]]、[[recordspeak-refactor-progress]]、[[redpacket-build-review-before-deploy]]。
