{
  "iteration": "merge-P0-modular-build-and-test-foundation",
  "date": "2026-06-09",
  "summary_one_line": "合并项目 P0 地基：源码分模块 + build.sh 拼接 → main.java + 端口/适配器 + 本地 bsh 测试 runner(含 sqlite-jdbc 真库)，用 speak-store UPSERT 缝做 red→green 打通全链路。先出设计文档待用户过目。",
  "user_confirmed": true,
  "requirements": [
    { "id": "R1", "priority": "must", "text": "设计文档(架构=端口与适配器 + 测试金字塔 + 分期 P0-P3)写入 docs/superpowers/specs/ 并提交；onboarding/README 记录工具(bsh-2.0b6.jar、sqlite-jdbc 来源)。用户过目后再建基建。" },
    { "id": "R2", "priority": "must", "text": "核实并记录 WAuxiliary getString/putString 的 config store 是否全宿主共享(决定 P2 迁移工作量)；记入设计文档/记忆。" },
    { "id": "R3", "priority": "must", "text": "仓库模块化结构 + build.sh：按声明顺序拼接 src 模块 + 选定生产适配器 → 单一可部署 main.java；产物过现有 bsh 离线解析(去 final EXIT=0)。" },
    { "id": "R4", "priority": "must", "text": "端口骨架 HostPort/StoragePort/Clock，各有生产(android/System)与测试(fake/sqlite-jdbc/假时钟)适配器；build 选生产集、test 选测试集。" },
    { "id": "R5", "priority": "must", "text": "本地测试 runner(run-tests.sh 跑 tests/*.bsh，失败非零退出)+ check.sh = build→bsh解析→单测→集成。证明缝=speak-store last_speak/first_seen UPSERT 抽成 StoragePort 模块，单测(fake)+集成(sqlite-jdbc 真库)断言 coalesce-NULL 语义，对旧 SQL red、对 coalesce SQL green。" }
  ],
  "design": {
    "summary": "P0 只搭地基+证明链路可行，不迁业务。端口与适配器把所有外部依赖(宿主 KV/发消息/成员表、SQLite、时钟)收敛成可替换缝；领域逻辑只依赖端口。build.sh 按 manifest 顺序 cat 源模块 + 选定适配器拼成 main.java。测试用 bsh-2.0b6.jar 本地执行 .bsh 测试脚本(与设备同解释器=高保真)，StoragePort 测试适配器用 sqlite-jdbc 跑真 SQLite(本地复现 max/min(NULL,x) 类 bug)。证明缝选 speak-store UPSERT：既打通 build+runner+jdbc 全链路，又把 v1.19.2/v1.20.0 的 coalesce 修复变成本地回归测试。",
    "risk_points": [
      { "id": "RISK1", "text": "拼接产物与手写单文件行为须一致：build.sh 拼接顺序(全局变量/函数声明)错会导致 BeanShell 未定义引用；产物必须过 bsh 解析 + 后续真机加载验证。P0 不部署业务，仅产物解析门禁。" },
      { "id": "RISK2", "text": "本地测试保真度：bsh 本地执行与设备 WAuxiliary 同解释器，差异仅在被 mock 的宿主/Android API；StoragePort 用 sqlite-jdbc 与 android SQLite 的 SQL 方言须一致(UPSERT/coalesce 标准 SQL，两端一致)。" },
      { "id": "RISK3", "text": "P0 不改动现有线上两个插件(GroupAdmin/RedPacketStats 照常运行)；地基在新目录独立推进，零触碰生产部署，避免影响在用群管/红包。" }
    ]
  },
  "traffic_flows": [
    { "id": "F1", "text": "构建路径：src 模块(manifest 顺序) + 生产适配器 → build.sh cat → main.java → bsh 解析门禁。" },
    { "id": "F2", "text": "测试路径：src 模块 + 测试适配器(fake host / sqlite-jdbc / 假时钟) → bsh 本地执行 tests/*.bsh → 断言 → 聚合 PASS/FAIL。" }
  ],
  "test_plan": [
    {
      "id": "T1",
      "scenario": "证明缝 单测(fake StoragePort)：speak-store upsertFirstSeen/upsertLastSpeak 对内存 fake 的行为。",
      "assertions": [
        "对已有更早 first_seen 取 min 不被覆盖；缺失行被插入",
        "对 NULL first_seen 行(coalesce 语义) → 被设为新值"
      ],
      "negative_or_boundary": true,
      "negative_case": "若实现退回 min(NULL,x) 语义致 NULL 留 NULL，单测须 red。",
      "covers": ["R5"],
      "risks": ["RISK2"],
      "traffic_flows": ["F2"]
    },
    {
      "id": "T2",
      "scenario": "证明缝 集成(真 sqlite-jdbc 库)：跑实际 UPSERT SQL。",
      "assertions": [
        "coalesce SQL：NULL→now、更早值保留、无行插入 三例全绿",
        "旧 SQL(min 无 coalesce)：NULL 行测试 red(复现历史 bug，证明测试有效)"
      ],
      "negative_or_boundary": true,
      "negative_case": "集成测试对旧 SQL 不 red = 测试没真连库/没断到点，FAIL。",
      "covers": ["R5"],
      "risks": ["RISK2"],
      "traffic_flows": ["F2"]
    },
    {
      "id": "T3",
      "scenario": "build.sh 拼接产物可解析。",
      "assertions": [
        "build.sh 输出单一 main.java，按 manifest 顺序拼接含选定生产适配器",
        "产物经 bsh.Parser 去 final 解析 EXIT=0"
      ],
      "negative_or_boundary": true,
      "negative_case": "拼接顺序错致未定义引用/解析失败 → check.sh red。",
      "covers": ["R3", "R4"],
      "risks": ["RISK1"],
      "traffic_flows": ["F1"]
    },
    {
      "id": "T4",
      "scenario": "交付物/事实核实 + 不触碰线上插件(验证检查，非 bsh 单测)。",
      "assertions": [
        "设计文档存在于 docs/superpowers/specs/ 且含架构+测试金字塔+分期；config store 共享性事实已记录(设计文档/记忆)",
        "git diff --name-only 显示 P0 期间 plugins/group-admin/main.java 与 plugins/redpacket-stats/main.java 零改动(线上不受影响)"
      ],
      "negative_or_boundary": true,
      "negative_case": "P0 改动了任一线上插件 main.java，或设计文档/config 事实缺失 → FAIL。",
      "covers": ["R1", "R2"],
      "risks": ["RISK3"],
      "traffic_flows": ["F1", "F2"]
    }
  ],
  "acceptance": [
    { "id": "A1", "text": "设计文档提交 + config store 事实核实记录(用户过目确认) + P0 不触碰线上插件。", "must_have_evidence": true, "tests": ["T3", "T4"], "covers": ["R1", "R2"] },
    { "id": "A2", "text": "build+端口+runner 地基跑通：证明缝 单测+集成 red→green，check.sh 全绿。", "must_have_evidence": true, "tests": ["T1", "T2", "T3"], "covers": ["R3", "R4", "R5"] }
  ],
  "tasks": [
    { "id": "W0", "stage": "EXECUTE", "title": "上设备核实 config store 共享性(只读诊断) + 写设计文档 docs/superpowers/specs/ + 暂停待用户过目", "covers": ["R1", "R2"], "risk": "low" },
    { "id": "W1", "stage": "EXECUTE", "title": "建仓库模块结构 + build.sh(manifest 拼接) + 端口骨架/适配器", "covers": ["R3", "R4"], "risk": "medium" },
    { "id": "W2", "stage": "EXECUTE", "title": "test runner + sqlite-jdbc + 证明缝 speak-store + 单测/集成 red→green + check.sh", "covers": ["R5"], "risk": "medium" }
  ],
  "irreversible_actions": [
    "P0 不部署、不触碰线上 GroupAdmin/RedPacketStats(照常运行)；仅在仓库新增 build/test 基建文件",
    "fetch sqlite-jdbc(一次性网络下载到本地 tools/，记入 README)"
  ],
  "out_of_scope": [
    "P1+ 业务迁移(GroupAdmin/RedPacketStats 进骨架)——P0 只搭地基 + 一个证明缝",
    "freeloader→doWarn 直连(P3)"
  ],
  "delivery": "P0 交付：设计文档(待用户过目)+ config store 核实 + 模块化 build + 端口/适配器 + 本地 bsh 测试 runner(sqlite-jdbc 真库)+ speak-store 证明缝 red→green。不动线上插件。"
}
